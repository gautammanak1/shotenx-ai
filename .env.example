# Repo root: used by docker-compose for frontend service env substitution only.
# For local dev, copy env files inside each package:
#   cp frontend/.env.example frontend/.env
#   cp backend/.env.example backend/.env

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
ASI_ONE_API_KEY=
AGENTVERSE_TOKEN=
